class UserData {
  constructor(user, folders, collections, cards, langs, cardcollections) {
    this.user = user;
    this.folders = folders;
    this.collections = collections;
    this.langs = langs;
  }
}

export default new UserData();
